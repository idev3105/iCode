{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog-allow",
  "description": "Allow dialog plugin to open directories",
  "windows": [
    "main"
  ],
  "permissions": [
    "dialog:allow-open"
  ]
}
